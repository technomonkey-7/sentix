services:
  vpn:
    image: qmcgaw/gluetun
    container_name: sentix_vpn
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      - VPN_SERVICE_PROVIDER=${VPN_SERVICE_PROVIDER:-protonvpn}
      - VPN_TYPE=${VPN_TYPE:-openvpn}
      # ProtonVPN or NordVPN credentials (set VPN_USER and VPN_PASSWORD in .env)
      - OPENVPN_USER=${VPN_USER:-your_openvpn_username_here}
      - OPENVPN_PASSWORD=${VPN_PASSWORD:-your_openvpn_password_here}
      # If using WireGuard protocol
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY:-}
      - FREE_ONLY=${VPN_FREE_ONLY:-on}
      - SERVER_COUNTRIES=${VPN_COUNTRIES:-Netherlands}
      - HTTPPROXY=on
    ports:
      - "8888:8888/tcp" # HTTP Proxy
      - "8388:8388/tcp" # SOCKS5 Proxy
    restart: always

  ui:
    build: .
    container_name: sentix_ui
    restart: always
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - NO_PROXY=localhost,127.0.0.1
      - no_proxy=localhost,127.0.0.1
    env_file:
      - .env
    depends_on:
      vpn:
        condition: service_started
      worker:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import requests; requests.get('http://localhost:8501/_stcore/health')\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  worker:
    build: .
    container_name: sentix_worker
    restart: always
    volumes:
      - .:/app
    environment:
      - NO_PROXY=localhost,127.0.0.1
      - no_proxy=localhost,127.0.0.1
    env_file:
      - .env
    command: python worker.py
    depends_on:
      vpn:
        condition: service_started
